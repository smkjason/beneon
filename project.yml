name: Jesus
options:
  bundleIdPrefix: com.smkjason
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "16.0"
  createIntermediateGroups: true

settings:
  base:
    SWIFT_VERSION: "5.0"
    TARGETED_DEVICE_FAMILY: "1,2"
    DEVELOPMENT_TEAM: ""

targets:
  Jesus:
    type: application
    platform: iOS
    sources:
      - path: Jesus/Sources
    resources:
      - path: Jesus/Resources
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.smkjason.Jesus
        INFOPLIST_FILE: Jesus/Resources/Info.plist
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        GENERATE_INFOPLIST_FILE: false
        CURRENT_PROJECT_VERSION: 1
        MARKETING_VERSION: 1.0.0
        CODE_SIGN_STYLE: Automatic
    scheme:
      testTargets: []
