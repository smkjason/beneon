import { PlaceholderPage } from "@/components/placeholder-page";

export default function CharacterPage() {
  return (
    <PlaceholderPage
      eyebrow="Formation"
      title="Character"
      description="Progress that reflects real growth — milestones and rewards tied to consistency in reading, prayer, and care for others."
    />
  );
}
